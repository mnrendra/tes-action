name: Push

on:
  push:

permissions:
  contents: read

jobs:
  # Tes Flow
  tes-flow:
    name: Tes Flow
    permissions:
      contents: write
    uses: ./.github/workflows/reusable.xyz.yml
    secrets:
      ASU: ${{ secrets.TOKEN }}
    # with:
    #   ABC: ${{ vars.ABC }}
    # runs-on: ubuntu-latest
    # steps:
    #   # tai
    #   - name: tai
    #     run: echo tai ${{ secrets.ASU }} asuuu
